# PROBE-1 -- diagnostic only. No expectations block: this is not pass/fail,
# it exists to see what the engine does with a shape it has never been
# shown. See tests/probes/coverage.md for the recorded run and verdict.
id: static-site
prompt: >
  Marketing site for our design studio. About 30,000 visitors a month,
  mostly from India. It's just pages and images, no login, no database.
  We want it cheap.
shape_expected: >
  S3 + CloudFront + Route 53. No VPC, no NAT, no database.
